class Phrase {
    constructor(phrase, translation, url, notes) {
        this.phrase = phrase;
        this.translation = translation;
        this.url = url;
        this.notes = notes;
    }
}

export default Phrase;